import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import TodayPlan from './TodayPlan';

ReactDOM.render(
  <React.StrictMode>
    <TodayPlan  name="hyunkis message"/>
  </React.StrictMode>,
  document.getElementById('root')
);
